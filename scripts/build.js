/**
 * Node script that fetches 1 025 Pokémon (name + types) and
 * writes an LZ‑string compressed payload into /js/data.js.
 *
 * Usage:
 *   npm install node-fetch lz-string
 *   node scripts/build.js
 */
console.log("Stub build script – fill in with fetch & compression logic.");
